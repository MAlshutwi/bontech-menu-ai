# URL of the deployed BonTech API. Leave empty only when this frontend is served by the same API origin.
VITE_MENU_API_URL=http://127.0.0.1:8000

# Add this later when the recommendation panel is connected to the AI API.
# VITE_AI_API_URL=https://your-api.example.com/recommendations
